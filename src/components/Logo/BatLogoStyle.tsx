import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  tinyLogo: {
    resizeMode: 'contain',
    height: 180,
  },
  titleText:{
    textAlign: 'center',
    fontWeight: 'bold',
    color: '#E5BF3C',
    fontSize:30,
  },
});